# Temporary staging copy

This folder mirrors the site at the repo root. **Delete this folder** once **www.m3pivot.com** is served from GitHub Pages.

**Test URL:** `https://msathiamurthy.github.io/m3pivot-website/option3/`  
(Requires the repo **without** a root `CNAME` file and no custom domain forced in GitHub **Settings → Pages**—otherwise GitHub redirects to `www`.)

**Local:** from repo root, `python3 -m http.server 8080` → open `http://localhost:8080/option3/`
